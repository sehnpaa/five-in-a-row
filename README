
## Init
# Git:
git clone https://github.com/sehnpaa/five-in-a-row/
cd five-in-a-row
git config --global user.email "you@example.com"
git config --global user.name "Your Name"
# gulp.js
npm install -g gulp
npm install --save-dev gulp
npm install --save-dev gulp-jshint
npm install --save-dev gulp-qunit
# Configure vim


gulp # in another terminal
python -m http.server # in another terminal
firefox localhost:8000
## Make changs
git pull
# Edit files
git add .
git status
git commit -m "commit message"
git status
git push -u origin master

# Revert last commit
git revert HEAD
git push -u origin master

# Delete files
git rm
